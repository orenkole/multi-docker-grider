version: "3"
services:
  client:
    image: "qb64kqjepetz1g/multiclient"
    mem_limit: 128m
    hostname: client
  server:
    image: "qb64kqjepetz1g/multiserver"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "qb64kqjepetz1g/multiworker"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "qb64kqjepetz1g/multinginx"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
